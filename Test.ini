sudo whoami
sudo -v
sudo ls /
sudo reboot
sudo -n cat /etc/shadow
sudo -u webadmin whoami
visudo
sudo - l
apt update
sudo -u **[username]** **[command]**
sudo -s
sudo -i
sudo -k
sudoedit /etc/hosts
sudo **visudo**
sudo **poweroff**
sudo **[command]**
sudo apt install **my-new-software**
sudo grep
